import 'package:flutter/material.dart';

class ModulerTestPage extends StatelessWidget {
  const ModulerTestPage({Key? key, required this.name}) : super(key: key);
  final String name;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
