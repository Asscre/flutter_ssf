import 'package:flutter/material.dart';

class UseDioPage extends StatelessWidget {
  const UseDioPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is UseDioPage'),
    );
  }
}
